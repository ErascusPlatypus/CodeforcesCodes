#include <bits/stdc++.h>
using namespace std;

int32_t main() {
    int t;
    cin >> t;

    while (t--) {
        string s ;
        cin >> s ;

        int n = s.length() ;
        if(n == 0) {
            cout << 0 << endl ;
            continue; 
        }
        if(n == 1) {
            cout << 1 << endl ;
            continue ;
        }

        for(int i = 0 ; i < n ; i++) {

        }
    }
    return 0;
}